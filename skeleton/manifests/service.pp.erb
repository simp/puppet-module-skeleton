# @summary Ensures services are running
# @api private
class <%= metadata.name %>::service {
  assert_private()

  service { $::<%= metadata.name %>::service_name:
    ensure     => running,
    enable     => true,
    hasstatus  => true,
    hasrestart => true
  }
}
