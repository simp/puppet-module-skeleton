# == Class <%= metadata.name %>::config::firewall
#
# This class is meant to be called from <%= metadata.name %>.
# It ensures that firewall rules are defined.
#
class <%= metadata.name %>::config::firewall {

  # FIXME: ensure yoour module's firewall settings are defined here.
  iptables::add_tcp_stateful_listen { 'allow_<%= metadata.name %>_tcp_connections':
    client_nets => $::<%= metadata.name %>::client_nets,
    dports      => $::<%= metadata.name %>::tcp_listen_port,
  }

}
